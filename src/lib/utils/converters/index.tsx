/* import type {
	DefaultNodeTypes,
	SerializedBlockNode,
} from '@payloadcms/richtext-lexical'

type NodeTypes =
	| DefaultNodeTypes
	| SerializedBlockNode<TableOfContentsProps | ContentWithMediaProps>

const jsxConverter = ({ defaultConverters }) => ({
	...defaultConverters,
    blocks: {
        
    }
}) */
